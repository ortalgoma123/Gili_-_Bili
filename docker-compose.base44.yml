services:
  web:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
      - base44-npm-cache:/root/.npm
    command: sh -c "npx --yes live-server@1.2.2 --host=0.0.0.0 --port=3000 --no-browser"
    ports:
      - "3000:3000"

volumes:
  base44-npm-cache:
